<%- include("partials/header") -%>
<main class="bobaFett">
        <div class="row d-flex justify-content-center align-items-center container-friends m-3 p-4">
            <h2 class="text-center">Liste d'ami·es</h2>
            <div class="col-lg-12 d-flex justify-content-between align-items-center">
                <% showFriends.forEach(e => { %>
                <div class="friends d-flex justify-content-around align-items-center m-1">
                    <img src="/img/icon.jpg" alt="icon utilisateur">
                    <span><%= e.name %></span>
                    <form action="" method="POST">
                        <button type="submit" class="btn btn-danger rounded-circle m-1 icons-ED d-flex justify-content-center align-items-center"><i
                                class="bi bi-x-circle"></i></button>
                    </form>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</main>
<%- include("partials/footer") -%>